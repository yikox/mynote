# 腾讯云-天数测试

<br />

镜像名称

<br />

```
docker run --name ${} -itd --privileged --cap-add=ALL --network=host --pid=host --ulimit memlock=-1:-1 --cap-add=sys_nice \
--entrypoint /bin/bash \
-w /data1/vipmt \
-v /dev:/dev \
-v /data1:/data1 \
${IMAGE}
```

