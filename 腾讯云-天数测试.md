# 腾讯云-天数测试

<br />

## 机器信息

```
ssh ubuntu@43.180.254.59 -p 34185
;xz%M}{)Pg6B9Z
```

驱动安装

### 安装必要包

```
yum install kernel-devel gcc make
或
apt install linux-headers-$(uname -r) gcc make
```

### cuda头文件

```
wget https://gitee.com/121786404/corex_knife/raw/master/driver/partial_install_cuda_header.tar.gz
tar xf partial_install_cuda_header.tar.gz
cd partial_install_cuda_header
bash install-cuda-header.sh 
```

### 驱动下载安装

```
sftp -P 29880  iluvatar_corex@iftp.iluvatar.com.cn (sftp -P 29880 iluvatar_corex@10.160.20.60)
密码c494vp8F9
get /client_tmp/vipmt/corex-installer-linux64-4.5.0.20260304_x86_64_10.2.run

bash corex-installer-linux64-4.5.0.20260304_x86_64_10.2.run
勾选Driver和Toolboox、Tookit ->进入Options-> 进入set insmod ilutar-> 写入itr_multi_compute_engine=1
wget https://gitee.com/121786404/corex_knife/raw/master/driver/corex.conf
cp corex.conf /etc/ld.so.conf.d/
echo "export PATH=\$PATH:/usr/local/corex/bin" >> /etc/profile
rm -f /usr/local/corex
ln -s /usr/local/corex-* /usr/local/corex
ldconfig
source /etc/profile
```

<br />

## 镜像

### 镜像下载

```
sftp -P 29880  iluvatar_corex@iftp.iluvatar.com.cn (sftp -P 29880 iluvatar_corex@10.160.20.60)
密码c494vp8F9
cd /client_tmp/vipmt/450
get corex-qwenimage-4.5.0.20260304.tar 
```

### docker 安装

```
# 1. 清理可能冲突的旧包
sudo apt remove -y docker.io docker-doc docker-compose docker-compose-v2 podman-docker containerd runc

# 2. 安装依赖
sudo apt update
sudo apt install -y ca-certificates curl

# 3. 添加 Docker 官方 GPG key
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

# 4. 添加 Docker 官方 apt 源
sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://download.docker.com/linux/ubuntu
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

# 5. 安装 Docker Engine + compose 插件
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

# 6. 启动并设置开机自启
sudo systemctl enable --now docker
```

### 镜像加载

```
docker load -i corex-qwenimage-4.5.0.20260304.tar
```

### 镜像启动

```
NAME=mt-qwenimage
IMAGE=crpi-92uj7jb20gffz04j.cn-guangzhou.personal.cr.aliyuncs.com/iluvatar_common/corex-qwenimage:4.5.0.20260304
docker run --name ${NAME} -itd --privileged --cap-add=ALL --network=host --pid=host --ulimit memlock=-1:-1 --cap-add=sys_nice \
--entrypoint /bin/bash \
-w /home/ubuntu \
-v /dev:/dev \
-v /home/ubuntu:/home/ubuntu \
${IMAGE}
docker exec -it  $NAME bash
```

<br />

