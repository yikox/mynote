# 四大开源大语言模型架构详解与 PD 分离部署

> 更新日期：2025年
> 涵盖模型：DeepSeek V4、GLM-5.1、MiniMax M2.7、Kimi K2.6

---

## 目录

- [1. 模型概览](#1-模型概览)
- [2. DeepSeek V4 架构详解](#2-deepseek-v4-架构详解)
- [3. GLM-5.1 架构详解](#3-glm-51-架构详解)
- [4. MiniMax M2.7 架构详解](#4-minimax-m27-架构详解)
- [5. Kimi K2.6 架构详解](#5-kimi-k26-架构详解)
- [6. MLA 多头潜在注意力详解](#6-mla-多头潜在注意力详解)
- [7. MoE 混合专家架构](#7-moe-混合专家架构)
- [8. PD 分离部署架构](#8-pd-分离部署架构)
- [9. 关键参数对比表](#9-关键参数对比表)

---

## 1. 模型概览

| 模型 | 总参数量 | 激活参数量 | Transformer 层数 | 隐藏维度 | 上下文长度 |
|------|---------|-----------|----------------|---------|-----------|
| **DeepSeek V4-Pro** | 1.6T | 49B | 61 | 7168 | 1M |
| **DeepSeek V4-Flash** | 284B | 13B | 43 | 4096 | 1M |
| **GLM-5.1** | 754B | 40B | 78 | 12288 | 200K |
| **MiniMax M2.7** | 456B | 45.9B | 32 | 4096 | 200K |
| **Kimi K2.6** | 1T | 32B | 61 | 7168 | 262K |

---

## 2. DeepSeek V4 架构详解

### 2.1 核心架构特点

- **MLA (Multi-head Latent Attention)**：低秩 KV 压缩，显著降低 KV Cache 显存
- **CSA + HCA 混合注意力**：Compressed Sparse Attention + Heavily Compressed Attention
- **mHC (Manifold-Constrained Hyper-Connections)**：新型残差连接，解决深层数值不稳定
- **DeepSeekMoE**：256/384 路由专家 + 1 共享专家，Top-6 激活

### 2.2 V4-Pro 完整数据流

```mermaid
flowchart TD
    subgraph Input["输入层"]
        A[输入文本] --> B[Tokenization<br/>BPE 128K vocab]
        B --> C[Embedding<br/>vocab_size → hidden=7168]
    end

    subgraph TransformerBlock["61 × Transformer Block"]
        direction LR
        D1["Block 0"] --> D2["Block 1"] --> D3["..."] --> D61["Block 60"]
    end

    subgraph SingleBlock["Block i 结构"]
        direction TB
        E[Pre-LayerNorm<br/>RMSNorm] --> F["MLA<br/>Multi-head Latent Attention"]
        F --> F1[残差连接]
        F1 --> G[Post-LayerNorm]
        G --> H["DeepSeekMoE FFN"]
        H --> I[残差连接<br/>Block_output]
    end

    subgraph MLA_Detail["MLA 内部结构"]
        J[Q/K/V 投影] --> J1["Latent Space<br/>dim=512"]
        J1 --> J2[RoPE on Q]
        J2 --> J3[Q@K^T/√d]
        J3 --> J4[Softmax]
        J4 --> J5[@V → O_proj]
        J5 --> J6[Output hidden=7168]
    end

    subgraph MoE_Detail["DeepSeekMoE 内部结构"]
        K[Router] --> K1[384 Expert Scores]
        K1 --> K2[Top-6 Selection]
        K2 --> K3["+ 1 Shared Expert<br/>所有Token经过"]
        K3 --> K4["每个专家<br/>intermediate=18432"]
    end

    C --> D1
    D61 --> L[Final RMSNorm]
    L --> M[LM Head]
    M --> N[Softmax]
    N --> O[输出 Token]

    style Input fill:#e1f5fe
    style TransformerBlock fill:#fff3e0
    style SingleBlock fill:#e8f5e9
    style MLA_Detail fill:#fce4ec
    style MoE_Detail fill:#f3e5f5
```

### 2.3 CSA + HCA 混合注意力详解

```mermaid
flowchart LR
    subgraph Standard["标准 Transformer 注意力"]
        A[Token 序列] --> B[Q, K, V]
        B --> C["O(n²) 计算<br/>所有Token对"]
        C --> D[输出]
    end

    subgraph CSA["CSA<br/>Compressed Sparse Attention"]
        E[Token 序列] --> F[轻量级索引器<br/>粗筛Token对]
        F --> G[稀疏结构<br/>可训练]
        G --> H[精选高密度<br/>计算区域]
    end

    subgraph HCA["HCA<br/>Heavily Compressed Attention"]
        I[Token 序列] --> J["MLA<br/>低维潜空间"]
        J --> K["推理时解压<br/>FP4+FP8混合精度"]
    end

    H --> L["效果"]
    K --> L
    L --> M["1M token: FLOPs → 27%<br/>KV Cache → 10%"]

    style Standard fill:#ffebee
    style CSA fill:#e3f2fd
    style HCA fill:#e8f5e9
    style L fill:#fff9c4
```

### 2.4 V4-Flash 配置

| 配置项 | 值 |
|--------|-----|
| Transformer 层数 | 43 |
| 隐藏维度 | 4096 |
| Query 头数 | 64 |
| KV 头数 | 1 (GQA) |
| 路由专家数 | 256 |
| 激活专家数 | 6 + 1 shared |
| CSA 压缩率 | 4 |
| HCA 压缩率 | 128 |

---

## 3. GLM-5.1 架构详解

### 3.1 核心架构特点

- **GlmMoeDSA**：Gated DeltaNet 线性注意力 + 标准注意力 + 稀疏 MoE
- **Dense-then-MoE 模式**：前 3 层密集 FFN 热身，后 75 层切换到 MoE
- **DeepSeek Sparse Attention**：借鉴 DeepSeek 稀疏注意力设计
- **MTP Head**：Multi-Token Prediction 推测解码加速

### 3.2 完整数据流

```mermaid
flowchart TD
    subgraph Input["输入层"]
        A[输入文本] --> B[Tokenization<br/>vocab ≈ 200K+]
        B --> C[Embedding<br/>vocab → hidden=12288]
    end

    subgraph DenseLayer["Block 0-2 (Dense 层)"]
        direction TB
        D0[Pre-LayerNorm] --> E0[标准 MHA<br/>64 heads]
        E0 --> R0[残差连接]
        R0 --> F0[Post-LayerNorm]
        F0 --> G0["Dense FFN<br/>intermediate=12288"]
        G0 --> H0[残差连接]
    end

    subgraph MoELayer["Block 3-77 (MoE 层)"]
        direction TB
        D1[Pre-LayerNorm] --> E1["GlmMoeDSA<br/>混合注意力"]
        E1 --> E2["MLA / Gated DeltaNet<br/>标准MHA"]
        E2 --> R1[残差连接]
        R1 --> F1[Post-LayerNorm]
        F1 --> G1["MoE FFN"]
        G1 --> H1[残差连接]
    end

    subgraph MoE_Detail["MoE FFN 内部"]
        G1 -->|放大| K[Router]
        K --> K1[256 Expert Scores]
        K1 --> K2[Top-8 Selection]
        K2 --> K3["+ 1 Shared Expert"]
    end

    subgraph Output["输出层"]
        H1 --> L[Final RMSNorm]
        L --> M[MTP Head]
        M --> N[LM Head]
        N --> O[Softmax → 输出]
    end

    C --> D0
    H0 --> D1
    H1 --> L

    style Input fill:#e1f5fe
    style DenseLayer fill:#fff8e1
    style MoELayer fill:#e8f5e9
    style Output fill:#fce4ec
```

### 3.3 Gated DeltaNet 结构

```mermaid
flowchart TD
    A[输入 x] --> B["门控网络<br/>gate = sigmoid(W_gate @ x)"]
    A --> C["Delta 计算<br/>delta = W_delta @ x"]

    B --> D["选择性通过<br/>gate × delta"]
    
    subgraph LinearAttn["线性注意力"]
        E[历史状态 s] --> F[A_t @ s + B_t @ x]
        F --> G[隐状态更新]
        G --> H["attn = C_t @ s"]
    end

    D --> I["(1 - gate) × 标准注意力"]

    D --> J[合并]
    I --> J
    J --> K[输出]

    style LinearAttn fill:#e3f2fd
    style J fill:#c8e6c9
```

### 3.4 关键配置

| 配置项 | 值 |
|--------|-----|
| Transformer 层数 | 78 |
| 隐藏维度 | 12288 |
| Attention Heads | 64 |
| KV Heads | 64 (Full attention) |
| 前 3 层 FFN | Dense (intermediate_size=12288) |
| 后 75 层 FFN | MoE (256 experts, top-8 routing) |
| 上下文长度 | 200K |

---

## 4. MiniMax M2.7 架构详解

### 4.1 核心架构特点

- **Lightning Attention + Softmax Attention**：混合注意力架构
- **32 个 MoE 专家**：总参数 456B，激活 45.9B
- **Full Attention + RoPE**：稳定长上下文处理
- **128K 滑动窗口**：适合长序列

### 4.2 完整数据流

```mermaid
flowchart TD
    subgraph Input["输入层"]
        A[输入文本] --> B[Tokenization<br/>vocab ≈ 200K]
        B --> C[Embedding<br/>vocab → hidden=4096]
    end

    subgraph TransformerBlock["32 × Transformer Block"]
        direction LR
        T1["Block 0"] --> T2["Block 1"] --> T3["..."] --> T32["Block 31"]
    end

    subgraph SingleBlock["Block i 结构"]
        direction TB
        D[Pre-LayerNorm] --> E["混合 Attention"]
        E --> E1["Lightning Attn<br/>线性 O(n)"]
        E --> E2["Softmax Attn<br/>标准全连接"]
        E1 & E2 --> F["RoPE<br/>旋转位置编码"]
        F --> R1[残差连接]
        R1 --> G[MoE FFN<br/>32 experts]
        G --> R2[残差连接]
    end

    subgraph HybridAttn["混合注意力详情"]
        E1 --> H1["State Space<br/>隐状态 s_t"]
        H1 --> H2["线性计算<br/>C_t @ s_t"]
        E2 --> H3["Q @ K^T → Softmax"]
    end

    subgraph Output["输出层"]
        T32 --> I[Final RMSNorm]
        I --> J[LM Head]
        J --> K[Softmax → 输出]
    end

    C --> T1
    T32 --> I

    style Input fill:#e1f5fe
    style TransformerBlock fill:#fff3e0
    style SingleBlock fill:#e8f5e9
    style HybridAttn fill:#f3e5f5
    style Output fill:#ffecb3
```

### 4.3 Lightning Attention 结构

```mermaid
flowchart LR
    subgraph StateSpace["State Space Model"]
        A[输入 x_t] --> B["B_t @ x_t"]
        A --> C["A_t @ s_{t-1}"]
        C --> D[状态更新]
        B --> D
        D --> E[隐状态 s_t]
        E --> F["C_t @ s_t"]
        F --> G[输出 attn(x_t)]
    end

    subgraph Comparison["与 Softmax Attention 对比"]
        H[Lightning] --> J[O(n) 线性]
        K[Softmax] --> L[O(n²)]
    end

    G --> M[合并输出]

    style StateSpace fill:#e3f2fd
    style J fill:#c8e6c9
    style L fill:#ffcdd2
```

### 4.4 关键配置

| 配置项 | 值 |
|--------|-----|
| Transformer 层数 | 32 |
| 隐藏维度 | 4096 |
| Attention Heads | 32 |
| KV Heads | 8 (GQA) |
| Head Dim | 128 |
| 专家数 | 32 |
| 上下文长度 | 200K |

---

## 5. Kimi K2.6 架构详解

### 5.1 核心架构特点

- **DeepSeek-V3 主干 + MLA**：继承 DeepSeek 的低秩压缩技术
- **原生多模态**：内置 MoonViT encoder，支持图像输入
- **384 路由专家 + 1 共享专家**：Top-8 + shared expert 架构
- **Agentic 能力**：工具调用、thinking mode、结构化输出

### 5.2 完整数据流

```mermaid
flowchart TD
    subgraph Multimodal["多模态输入 (可选)"]
        A1[文本] --> D[融合]
        A2[图像] --> B[MoonViT Vision Encoder]
        B --> C[patches → vision_hidden]
        C --> D
    end

    subgraph Input["输入层"]
        D --> E[Tokenization<br/>vocab ≈ 200K+]
        E --> F[Embedding<br/>vocab → hidden=7168]
    end

    subgraph TransformerBlock["61 × Transformer Block"]
        direction LR
        T1["Block 0"] --> T2["Block 1"] --> T3["..."] --> T61["Block 60"]
    end

    subgraph SingleBlock["Block i 结构"]
        direction TB
        G[Pre-LayerNorm] --> H["MLA"]
        H --> H1["Q: 64 heads<br/>dim=112"]
        H --> H2["KV: latent=512<br/>低秩压缩"]
        H2 --> H3[RoPE on Q]
        H3 --> H4["Q @ K^T → Softmax → @ V"]
        H4 --> H5[O_proj → hidden=7168]
        H5 --> R1[残差连接]
        R1 --> I[MoE FFN]
        I --> R2[残差连接]
    end

    subgraph MoE_Detail["MoE FFN"]
        I -->|放大| M[Router]
        M --> M1[384 Expert Scores]
        M1 --> M2[Top-8 + 1 shared]
    end

    subgraph Agentic["Agentic 能力"]
        N[LM Head] --> O[Tool Calling]
        N --> P[Thinking Mode]
        N --> Q[Structured Output]
        N --> R[Agent Swarm]
    end

    subgraph Output["输出"]
        T61 --> S[Final LayerNorm]
        S --> N
        N --> Y[Softmax → 输出]
    end

    F --> T1
    T61 --> S

    style Multimodal fill:#fce4ec
    style Input fill:#e1f5fe
    style TransformerBlock fill:#fff3e0
    style SingleBlock fill:#e8f5e9
    style MoE_Detail fill:#f3e5f5
    style Agentic fill:#fff9c4
```

### 5.3 关键配置

| 配置项 | 值 |
|--------|-----|
| Transformer 层数 | 61 |
| 隐藏维度 | 7168 |
| Attention Heads | 64 |
| Head Dim | 112 |
| 路由专家数 | 384 |
| 激活专家数 | 8 + 1 shared |
| 上下文长度 | 262.1K |
| 多模态 | Native (MoonViT) |

---

## 6. MLA 多头潜在注意力详解

### 6.1 标准 MHA vs MLA

```mermaid
flowchart LR
    subgraph StandardMHA["标准 Multi-Head Attention (MHA)"]
        A1[输入 x] --> B1[Q_proj]
        A1 --> B2[K_proj]
        A1 --> B3[V_proj]
        B1 --> C1["[batch, seq<br/>num_heads<br/>head_dim]"]
        B2 --> C2["[batch, seq<br/>num_heads<br/>head_dim]"]
        B3 --> C3["[batch, seq<br/>num_heads<br/>head_dim]"]
        C1 & C2 --> D1["Q @ K^T"]
        D1 --> D2[Softmax]
        D2 & C3 --> D3["@ V"]
        D3 --> E1[O_proj]
        E1 --> F1[输出]
        
        F1 --> G1["❌ KV Cache 大<br/>64 heads × 128 dim × 2<br/>= 16KB/token/layer"]
    end

    subgraph MLA["MLA (Multi-head Latent Attention)"]
        A2[输入 x] --> B4[Q_proj]
        A2 --> B5[K_latent_proj]
        A2 --> B6[V_latent_proj]
        B5 --> C4["[seq, latent=512]<br/>压缩存储"]
        B6 --> C5["[seq, latent=512]<br/>压缩存储"]
        B4 --> C6["Q with RoPE"]
        C4 & C6 --> D4[推理时解压]
        D4 --> D5[K]
        C5 & D5 --> D6["Q @ K^T → Softmax → @ V"]
        D6 --> E2[输出]
        
        E2 --> G2["✅ KV Cache 小<br/>512 × 2<br/>= 1KB/token/layer"]
    end

    style StandardMHA fill:#ffebee
    style MLA fill:#e8f5e9
    style G1 fill:#ffcdd2
    style G2 fill:#c8e6c9
```

### 6.2 KV Cache 大小对比

```mermaid
flowchart LR
    subgraph Table["KV Cache 对比"]
        A["标准 MHA<br/>64 heads × 128 dim"] --> B["16 KB/token/layer"]
        C["MLA (DeepSeek V4)<br/>latent_dim=512"] --> D["1 KB/token/layer"]
    end

    subgraph Calculation["1000 tokens, 61 layers 计算"]
        E["标准: 16KB × 1000 × 61"] --> F["≈ 976 MB"]
        G["MLA: 1KB × 1000 × 61"] --> H["≈ 61 MB"]
    end

    F --> I["压缩比 ≈ 16x"]
    H --> I

    style A fill:#ffcdd2
    style C fill:#c8e6c9
    style F fill:#ffcdd2
    style H fill:#c8e6c9
    style I fill:#fff9c4
```

---

## 7. MoE 混合专家架构

### 7.1 MoE 结构

```mermaid
flowchart TD
    A[输入 x] --> B["Router<br/>门控网络"]

    B --> C["scores → softmax<br/>→ Top-K indices"]

    subgraph Experts["N 个 Expert FFN"]
        C --> E0[Expert 0]
        C --> E1[Expert 1]
        C --> E2[Expert 2]
        C --> E3[...]
        C --> EN[Expert N]
    end

    E0 --> O0[output_0]
    E1 --> O1[output_1]
    E2 --> O2[output_2]
    EN --> ON[output_N]

    subgraph Merge["加权求和"]
        O0 & O1 & O2 & ON --> SUM
        SUM --> OUTPUT
    end

    style B fill:#e3f2fd
    style Experts fill:#fff8e1
    style Merge fill:#c8e6c9
```

### 7.2 各模型 MoE 配置

```mermaid
flowchart LR
    subgraph Config["MoE 配置对比"]
        A["DeepSeek V4-Pro<br/>384 routed + 1 shared<br/>Top-6"] 
        B["DeepSeek V4-Flash<br/>256 routed + 1 shared<br/>Top-6"]
        C["GLM-5.1<br/>256 routed + 1 shared<br/>Top-8"]
        D["MiniMax M2.7<br/>32 experts"]
        E["Kimi K2.6<br/>384 routed + 1 shared<br/>Top-8"]
    end

    style A fill:#e3f2fd
    style B fill:#e3f2fd
    style C fill:#fff8e1
    style D fill:#c8e6c9
    style E fill:#fce4ec
```

### 7.3 MoE 的资源优势

```mermaid
flowchart TD
    subgraph Stats["DeepSeek V4-Pro 示例"]
        A["总参数量: 1.6T"] 
        B["激活参数量: 49B<br/>(约 3%)"]
    end

    subgraph Benefit["优势"]
        B --> C["推理成本 = 49B 模型成本<br/>而非 1.6T"]
        B --> D["每个专家独立学习<br/>不同知识领域"]
        B --> E["存储空间大<br/>计算量小"]
    end

    style Stats fill:#fff9c4
    style C fill:#c8e6c9
    style D fill:#c8e6c9
    style E fill:#c8e6c9
```

---

## 8. PD 分离部署架构

### 8.1 为什么需要 PD 分离

| 阶段 | 计算特点 | 资源需求 | 瓶颈 |
|------|---------|---------|------|
| **Prefill** | 密集计算，批量矩阵运算 | GPU 计算资源 | 算力 |
| **Decode** | 内存密集，KV Cache 读写 | GPU 显存带宽 | 显存容量 |

### 8.2 PD 分离架构图

```mermaid
flowchart TD
    A[用户请求] --> B[API Gateway<br/>请求路由分发]

    B --> C[Prefill 节点群]
    B --> D[Decode 节点群]

    subgraph Prefill["Prefill 节点群"]
        C1["Batch Prefill<br/>批量并行计算"] 
        C1 --> C2[KV Cache 生成]
        C2 --> C3[KV Cache 传输<br/>到 Decode 节点]
    end

    subgraph Decode["Decode 节点群"]
        D1[接收 KV Cache<br/>+ 首 Token]
        D1 --> D2["Decode 循环<br/>逐 Token 生成"]
        D2 --> D3[Streaming Output<br/>流式输出]
    end

    C3 -.->|RDMA/高速网络| D1

    style C fill:#e3f2fd
    style D fill:#fff8e1
    style C3 fill:#fce4ec
    style D1 fill:#fce4ec
```

### 8.3 单请求完整数据流

```mermaid
sequenceDiagram
    participant User as 用户
    participant API as API Gateway
    participant Prefill as Prefill 节点
    participant Decode as Decode 节点

    User->>API: "请写一首关于春天的诗"

    rect rgb(227, 242, 253)
        Note over Prefill: Prefill 阶段
        API->>Prefill: 分发请求
        Prefill->>Prefill: Tokenization → 100+ tokens
        Prefill->>Prefill: Embedding → hidden=7168
        loop 61 × Transformer Block
            Prefill->>Prefill: 批量计算注意力
            Prefill->>Prefill: Top-6 专家 MoE
        end
        Prefill->>Prefill: 生成完整 KV Cache
    end

    Prefill->>Decode: KV Cache + 首 Token<br/>RDMA 传输

    rect rgb(255, 243, 224)
        Note over Decode: Decode 阶段
        loop 直到 EOS
            Decode->>Decode: 读取 KV Cache
            Decode->>Decode: MLA(x, kv_cache)
            Decode->>Decode: 更新 KV Cache
            Decode->>Decode: LM_Head → softmax
            Decode->>Decode: 输出 next_token
            Decode-->>User: 流式响应
        end
    end
```

### 8.4 KV Cache 传输计算

```mermaid
flowchart TD
    subgraph Input["配置"]
        A["layers = 61<br/>seq_len = 2000<br/>kv_heads = 1<br/>head_dim = 128<br/>dtype = BF16"]
    end

    subgraph Calc1["单层单 token KV"]
        B["K: 1×128 = 256B<br/>V: 1×128 = 256B"]
        C["合计: 512 bytes"]
    end

    subgraph Calc2["完整序列"]
        D["512 × 2000 × 61"]
        D --> E["= 62.5 MB"]
    end

    subgraph Calc3["100 并发请求"]
        F["62.5 MB × 100"]
        F --> G["= 6.25 GB"]
    end

    subgraph Calc4["传输时间"]
        H["400 Gbps RDMA"]
        G --> I["6.25 GB ÷ 50 GB/s"]
        I --> J["≈ 125 ms"]
    end

    style Input fill:#e3f2fd
    style Calc1 fill:#fff8e1
    style Calc2 fill:#c8e6c9
    style Calc3 fill:#fce4ec
    style Calc4 fill:#fff9c4
```

### 8.5 Prefill vs Decode 节点硬件配置

```mermaid
flowchart LR
    subgraph Prefill["Prefill 节点群"]
        A["目标: 高吞吐批量计算"]
        A1["硬件: H100/A100<br/>高算力 GPU"]
        A2["显存: 80GB+<br/>相对较小"]
        A3["策略: Continuous Batching<br/>Paged Attention<br/>TP=8"]
    end

    subgraph Decode["Decode 节点群"]
        B["目标: 低延迟长序列服务"]
        B1["硬件: H100<br/>大显存容量"]
        B2["显存: 必须大<br/>存储大量 KV Cache"]
        B3["策略: Dynamic Batching<br/>Prefix Caching<br/>Speculative Decoding"]
    end

    style Prefill fill:#e3f2fd
    style Decode fill:#fff8e1
```

### 8.6 PD 分离变体

#### Prefix Caching 优化

```mermaid
flowchart TD
    A["系统提示"] --> B[Prefill<br/>一次性计算]

    B --> C["KV Cache 共享"]

    C --> D[Decode A]
    C --> E[Decode B]
    C --> F[Decode C]

    style B fill:#c8e6c9
    style C fill:#fff9c4
    style D fill:#e3f2fd
    style E fill:#e3f2fd
    style F fill:#e3f2fd
```

#### 投机解码配合 PD 分离

```mermaid
sequenceDiagram
    participant P as Prefill 节点
    participant D as Decode 节点
    participant U as 用户

    P->>D: 推测 Token [t1,t2,t3,t4,t5]
    P->>D: + KV Cache

    rect rgb(232, 245, 233)
        Note over D: 快速验证
        D->>D: 验证 t1 ✓
        D->>D: 验证 t2 ✓
        D->>D: 验证 t3 ✓
        D->>D: 验证 t4 ✗ 回退
        D->>D: 生成正确分支
    end

    D-->>U: 流式输出<br/>(接受率 ~80-90%)
```

---

## 9. 关键参数对比表

| 参数 | DeepSeek V4-Pro | DeepSeek V4-Flash | GLM-5.1 | MiniMax M2.7 | Kimi K2.6 |
|------|-----------------|------------------|---------|--------------|-----------|
| **总参数量** | 1.6T | 284B | 754B | 456B | 1T |
| **激活参数量** | 49B | 13B | 40B | 45.9B | 32B |
| **Transformer 层数** | 61 | 43 | 78 | 32 | 61 |
| **隐藏维度** | 7168 | 4096 | 12288 | 4096 | 7168 |
| **Attention Heads** | 128 | 64 | 64 | 32 | 64 |
| **Head Dimension** | 512 | 512 | 192 | 128 | 112 |
| **KV Heads (GQA)** | 1 | 1 | 64 | 8 | 1 |
| **MLA latent_dim** | 512 | 512 | 512 | N/A | 512 |
| **MoE 专家总数** | 384 | 256 | 256 | 32 | 384 |
| **激活专家数** | 6 + 1 shared | 6 + 1 shared | 8 + 1 shared | - | 8 + 1 shared |
| **上下文长度** | 1M | 1M | 200K | 200K | 262K |
| **多模态** | ❌ | ❌ | ❌ | ❌ | ✅ |
| **注意力类型** | MLA + CSA/HCA | MLA + CSA/HCA | MLA + Gated DeltaNet | Lightning + Softmax | MLA |

---

## 附录: 术语表

| 术语 | 含义 |
|------|------|
| MLA | Multi-head Latent Attention，低秩 KV 压缩注意力 |
| MoE | Mixture of Experts，混合专家架构 |
| GQA | Grouped Query Attention，分组查询注意力 |
| RoPE | Rotary Position Embedding，旋转位置编码 |
| CSA | Compressed Sparse Attention，压缩稀疏注意力 |
| HCA | Heavily Compressed Attention，重度压缩注意力 |
| mHC | Manifold-Constrained Hyper-Connections，流形约束超连接 |
| MTP | Multi-Token Prediction，多 token 预测 |
| PD | Prefill-Decode，推理两阶段分离 |
| KV Cache | Key-Value Cache，注意力计算的缓存 |
| FFN | Feed-Forward Network，前馈网络 |